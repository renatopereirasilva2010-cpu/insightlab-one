API_URL=http://localhost:4000
# Mesma API, mas legivel pelo browser (Server Components usam API_URL acima;
# fotos/logo enviados via upload sao <img>/<Image> renderizados no client,
# que precisa de URL absoluta e publica pro host da API).
NEXT_PUBLIC_API_URL=http://localhost:4000
